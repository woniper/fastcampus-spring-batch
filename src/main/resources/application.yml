spring:
  datasource:
    driver-class-name: org.h2.Driver
    data: classpath:person.sql
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: create
  batch:
    job:
      names: ${job.name:NONE}
